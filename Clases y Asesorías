<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Clases y Asesorías</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: white;
      font-family: Arial, sans-serif;
    }

    .header {
      background-color: #CE93D8;
      color: white;
      padding: 15px;
      font-size: 20px;
      font-weight: bold;
    }

    .container {
      display: flex;
      justify-content: space-between;
      padding: 20px;
    }

    .section {
      width: 48%;
    }

    .title {
      font-size: 22px;
      font-weight: bold;
      color: #622C71;
      margin-bottom: 10px;
    }

    .buttons-container {
      display: flex;
      gap: 10px;
      margin-bottom: 10px;
    }

    .buttons-container button {
      background-color: #FFE699;
      border: none;
      border-radius: 50%;
      width: 35px;
      height: 35px;
      font-size: 18px;
      cursor: pointer;
    }

    .card {
      display: flex;
      background-color: #CE93D8;
      color: white;
      margin-bottom: 10px;
      border-radius: 8px;
      overflow: hidden;
    }

    .card img {
      width: 40%;
      object-fit: cover;
    }

    .card-info {
      padding: 10px;
      width: 60%;
    }

    .card-info h3 {
      color: #FFE699;
      margin: 0 0 5px 0;
    }

    .card-info p {
      margin: 2px 0;
    }

    /* Materias estilo tabla */
    .materias-table {
      display: grid;
      grid-template-columns: 1fr 2fr;
      gap: 5px;
      margin-top: 10px;
    }

    .materias-header {
      background-color: #622C71;
      color: white;
      font-weight: bold;
      padding: 8px;
      text-align: center;
      border-radius: 4px;
    }

    .materias-grid {
      display: contents;
    }

    .cell {
      background-color: #FFE699;
      padding: 10px;
      border-radius: 4px;
      color: black;
    }
  </style>
</head>
<body>
  <div class="header">👤 Bienvenido @asesor</div>

  <div class="container">
    <!-- Asesorías -->
    <div class="section">
      <div class="title">Asesorías</div>
      <div class="buttons-container">
        <button onclick="agregarAsesoria()">➕</button>
        <button onclick="borrarAsesorias()">🗑️</button>
        <button onclick="buscarAsesoria()">🔍</button>
      </div>
      <div id="asesorias-container"></div>
    </div>

    <!-- Materias -->
    <div class="section">
      <div class="title">Materias</div>
      <div class="buttons-container">
        <button onclick="agregarMateria()">➕</button>
        <button onclick="borrarMaterias()">🗑️</button>
        <button onclick="buscarMateria()">🔍</button>
      </div>
      <div class="materias-table">
        <div class="materias-header">Nombre</div>
        <div class="materias-header">Descripción</div>
        <div id="materias-container" class="materias-grid"></div>
      </div>
    </div>
  </div>

  <script>
    let asesorias = [
      {
        nombre: "MATEMÁTICAS",
        horario: "3:00 a 4:00 pm",
        asesor: "@usuario",
        modalidad: "Presencial",
        imagen: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQN9jZ7n8rUMxSK27j7UXQu79JhXabGS3CnmQ&s"
      },
      {
        nombre: "QUÍMICA",
        horario: "3:00 a 4:00 pm",
        asesor: "@usuario",
        modalidad: "Presencial",
        imagen: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcR_nfc2IxR-RG4nI8XBWskpL7qMfcxyhZjHMg&s"
      }
    ];

    let materias = [
      {
        nombre: "Matemáticas I",
        descripcion: "Introducción al álgebra, trabajando con ecuaciones, expresiones y funciones básicas."
      },
      {
        nombre: "Química I",
        descripcion: "Estudia la estructura de la materia, enlaces químicos y reacciones básicas."
      }
    ];

    function renderizarAsesorias() {
      const contenedor = document.getElementById("asesorias-container");
      contenedor.innerHTML = "";
      asesorias.forEach((a) => {
        contenedor.innerHTML += `
          <div class="card">
            <img src="${a.imagen}" alt="imagen">
            <div class="card-info">
              <h3>${a.nombre}</h3>
              <p><strong>Horario:</strong> ${a.horario}</p>
              <p><strong>Asesor:</strong> ${a.asesor}</p>
              <p><strong>Modalidad:</strong> ${a.modalidad}</p>
            </div>
          </div>`;
      });
    }

    function renderizarMaterias() {
      const contenedor = document.getElementById("materias-container");
      contenedor.innerHTML = "";
      materias.forEach(m => {
        contenedor.innerHTML += `
          <div class="cell">${m.nombre}</div>
          <div class="cell">${m.descripcion}</div>`;
      });
    }

    function agregarAsesoria() {
      const nombre = prompt("Nombre de la asesoría:");
      const horario = prompt("Horario:");
      const modalidad = prompt("Modalidad:");
      const asesor = prompt("Asesor:");
      if (nombre && horario && modalidad && asesor) {
        asesorias.push({
          nombre,
          horario,
          modalidad,
          asesor,
          imagen: "https://via.placeholder.com/150"
        });
        renderizarAsesorias();
      }
    }

    function agregarMateria() {
      const nombre = prompt("Nombre de la materia:");
      const descripcion = prompt("Descripción:");
      if (nombre && descripcion) {
        materias.push({ nombre, descripcion });
        renderizarMaterias();
      }
    }

    function borrarAsesorias() {
      asesorias = [];
      renderizarAsesorias();
    }

    function borrarMaterias() {
      materias = [];
      renderizarMaterias();
    }

    function buscarAsesoria() {
      const consulta = prompt("Buscar asesoría por nombre:");
      if (!consulta) return;
      const resultado = asesorias.filter(a => a.nombre.toLowerCase().includes(consulta.toLowerCase()));
      if (resultado.length > 0) {
        alert("Se encontraron " + resultado.length + " coincidencias.");
      } else {
        alert("No se encontró ninguna asesoría con ese nombre.");
      }
    }

    function buscarMateria() {
      const consulta = prompt("Buscar materia por nombre:");
      if (!consulta) return;
      const resultado = materias.filter(m => m.nombre.toLowerCase().includes(consulta.toLowerCase()));
      if (resultado.length > 0) {
        alert("Se encontraron " + resultado.length + " coincidencias.");
      } else {
        alert("No se encontró ninguna materia con ese nombre.");
      }
    }

    renderizarAsesorias();
    renderizarMaterias();
  </script>
</body>
</html>
